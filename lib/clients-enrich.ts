/** No-op conservé tant que les chantiers n’exportent pas d’enrichissement client côté API. */
export async function enrichClientFromChantier(clientId: string, adresseChantier: string | null) {
  void clientId;
  void adresseChantier;
}
